import 'package:flutter/material.dart';

class PartyCreationScreenDetails extends StatelessWidget {
  const PartyCreationScreenDetails({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('DETAILS SCREEN'),
      ),
    );
  }
}
